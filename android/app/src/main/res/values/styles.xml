<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Android 12+ Splash Screen -->
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@raw/pushpro_2</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/launch_screen_icon</item>
        <item name="android:windowBackground">@color/launch_screen_icon</item>
    </style>

    <!-- Custom Splash Theme for Android 11 and below -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@layout/launch_screen</item>
        <item name="android:statusBarColor">@color/launch_screen_icon</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
